# Shipping setup

Shipping classes are defined in `src/data/shipping-classes.ts`.

Demonstration rates are labeled as demonstration only. Live carrier integration should replace demo rates when configured. Items with `shipping-review-required` must request a quote.
